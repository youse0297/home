# RenderDoc Capture Drop

This directory is reserved for the binary capture artifact:

`MaterialLab_Frame_0001.rdc`

The current machine does not have RenderDoc installed, so no placeholder `.rdc` is committed. Run `Tools/RenderDocCaptureCheck.ps1` after installing RenderDoc; then open Unity, run `TA/Material Lab/Prepare RenderDoc Capture`, capture one frame at 1280x720, and save it here. The marker names and event boundaries are recorded in `Assets/_TA/Documentation/RenderDocCaptureManifest.json`.
