## 环境要求
- C++17 编译器
- CMake 3.10+
- 仅依赖标准库

# 开发与构建环境
本项目使用以下工具链：
- 语言标准：C++17
- 构建系统：CMake 3.10+
- 编译器：GCC、Clang 或 MSVC（均兼容）
- 平台：跨平台（Windows/Linux/macOS）

所有依赖均来自 C++ 标准库，无需额外安装。

# VecMath —— C++ 二维/三维向量库

轻量级控制台工程，实现了 2D/3D/4D 向量和 4x4 矩阵的基本运算，包含加减、数乘、归一化、点积、叉积、投影、夹角、侧向判断、矩阵乘法、矩阵×向量以及常用变换矩阵（平移、缩放、旋转）。

## 坐标系约定
- 默认右手系，可在运行时通过 `g_handedness` 切换至左手系。
- 所有几何计算（侧向、投影等）自动适配当前坐标系。

# VecMath —— C++ 向量/矩阵数学库

轻量级控制台工程，实现了 2D/3D/4D 向量和 4x4 矩阵的基本运算，包含加减、数乘、归一化、点积、叉积、投影、夹角、侧向判断、矩阵乘法及矩阵×向量等。

## 构建与运行

```bash
mkdir build && cd build
cmake ..
make
./vecmath
```

## 使用示例

```cpp
Vec2 a(1, 2), b(4, -1);
Vec2 c = a + b;            // 加法
Vec2 d = a - b;            // 减法
Vec2 e = a * 2.5;          // 数乘
Vec2 f = 3.0 * a;          // 标量乘向量
Vec2 g = a.normalized();   // 归一化（返回单位向量）

std::cout << a << " + " << b << " = " << c << std::endl;
```

`Vec3` 用法完全一致。

## 设计要点

- 运算符重载：`+`、`-`、`*`（标量）、`/`（标量），支持 `+=`、`-=`、`*=`、`/=`
- 归一化：`normalized()`（不修改原对象）和 `normalize()`（原地修改）
- 零向量归一化返回零向量，除零抛出异常
- 仅依赖 C++17 标准库

## 文件结构

```
src/
  Vec2.hpp / Vec2.cpp
  Vec3.hpp / Vec3.cpp
  main.cpp
CMakeLists.txt
```
