# 脱敏简历正文

> 方括号内容必须由本人填写；完成后在私有位置导出 PDF，不在公共仓库保存电话和私人邮箱。

## [姓名]｜[目标岗位]

[手机] ｜ [邮箱] ｜ [所在城市] ｜ [作品/仓库链接（公网复验后填写）]

## 教育经历

**[学校]｜[专业]｜[学历]**　[入学年月]—[毕业年月]

- [仅填写与岗位相关且可核实的课程、奖项或排名；没有则删除本行]

## 专业能力

- C++17、CMake、HLSL；图形变换、软件光栅化、纹理采样与 PBR/GGX 基础。
- Unity `2022.3.62f3c1`、URP `14.0.12`；材质输入、BasePass 拆解、Shader 模块化与自动化验证。
- PowerShell、CTest、JSON 验收报告、确定性发布包与 SHA-256 校验。

## 项目经历

### C++17 CPU 软渲染器　[项目起止年月]

`C++17 / CMake / OBJ / 光栅化 / PBR / 自动化测试`

- 构建从 OBJ 到 P6 RGB8 图像的 CPU 软渲染管线，贯通 MVP 变换、top-left 光栅化、透视正确插值、深度与纹理采样，并实现 Lambert 和 GGX 金属粗糙度直接光。
- 建立 `12` 项 CTest 自动验收，以 `96×32` 三材质基准图、`1872` 个材质覆盖像素及 FNV-1a64 checksum 固定输出，发布门禁 `12/12` 通过。

### Unity URP HLSL 材质源码库 v1.0　[项目起止年月]

`Unity 2022.3 / URP 14 / HLSL / GGX / 材质系统 / 自动化验证`

- 拆分并封装 `18` 个 Renderer HLSL 功能模块，以统一材质接口组织采样、PBR 输入、GGX 光照、顶点变形、各向异性和透明折射；发布包包含 `19` 个 HLSL 文件、`74` 个公共符号。
- 建立空工程迁移与确定性发布流程，完成 `18/18` 个专项验证器、`116` 项源码结构检查，并以 Windows SDK `fxc /WX /Ges` 完成 smoke shader 零警告编译。

## 补充实践（版面不足时整节删除）

### Unity URP 材质与管线分析基线

- 建立 `11` 组材质参数边界样例和 `10` 档 BasePass 表面/光照拆解视图；在 `64×64` 全 mip 格式字节基准中，BC1 相对 RGBA32 节省 `87.4%`，BC5/BC7 节省 `74.9%`。

## 投递前删除本节

- 不要将格式字节基准改写为“项目总显存下降”。
- 不要将静态 LOD 样本改写为“FPS 提升”。
- 不要将 `fxc` smoke 编译改写为 Unity 全平台 shader variant 通过。
- 不要将离线分析板标注为 Unity 运行截图或 RenderDoc 捕获。

